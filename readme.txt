check: 'sufficiently close' (runs too long)
check 'support' area for structure tensor
line threshholds; falloff.

I should make them scriptable and just let the fucking thing run with many parameters.

In fact, this would be a good reason to have quad CPU.

1AGW's true sample resolution. it's fineness could be a source of slowness, as well.
